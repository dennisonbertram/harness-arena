// ponytail: RED-commit stub — returns input unchanged so the sort/filter
// tests fail on assertions. Replaced in the green commit.
import type { Run } from "./types";

export function sortLeaderboard(runs: Run[]): Run[] {
  return runs;
}
